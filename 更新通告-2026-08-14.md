# Lattice Launcher 更新通告

**版本：** v1.0.1（2026-08-14 迭代）
**状态：** 已发布，安装包签名有效（Authenticode Valid）
**获取：** 安装包已就绪，随时可分发

---

## 本次更新内容

### 下载引擎

**1. 陪跑（Pace Runner）——主动换路，不等判死**

下载竞速胜出的源开始降速时，启动器实时监测其速度。连续 5 秒下降且低于窗口峰值一半，立即在后台重赛 2 个历史最快的落选源；新源反超并稳定领先 3 秒后自动顶替旧源。此前需要等待「判死 30 秒 → 重赛 → 重新起跑」的交替空窗，现在新源提前就位，下载不中断。触发判定取窗口次高值，防止开局瞬时速度造成误判。

**2. Forge 整合包下载修复**

`maven.minecraftforge.net` 此前未接入镜像，Forge 安装器单候选直连，国内实测仅 37-81KB/s 并触发判死失败。现已接入 BMCLAPI 镜像，官方与镜像并行竞速，慢的一方自动让路。

**3. 完整性校验补全**

此前 natives（运行时）库不参与文件校验，删掉相关文件后「修复」仍报告完整，启动时才暴露错误。现已补齐，质检结果与实际启动状态一致。

### 版本管理

**4. 版本设置新增 Java 版本选择**

自动扫描本机已安装的 JDK/JRE，版本详情页一键下拉选择，选中即写入该版本配置；自定义路径输入保留。

**5. 26.2 版本实例显示修复**

原版 jar 位于加载器子目录时，已装版本判定漏标。修正判定逻辑（自身/父版本/引用子目录三路检测），版本列表与主页补漏统一口径。

**6. 资源包 / 光影包管理页**

版本详情页新增分区：已安装的资源包与光影包可启停（.disabled）、删除、打开所在文件夹。此前仅支持下载、无处管理。

**7. 模组更新检查**

已安装模组行新增「检查更新」：查询 Modrinth 最新版本并对比版本号，有新版一键下载替换（SHA1 校验）。状态如实反馈（已是最新 / 未找到 / 检查失败）。

### 账号与皮肤

**8. 正版登录交互修复**

发起登录后立即显示连接状态，登录全流程（设备码 → 授权 → 认证链）逐步反馈，不再出现「点击无反应」的体感。

**9. 皮肤重置改为强制同步**

正版账号重置皮肤改为从正版账号强制同步官方皮肤（镜像拉取，国内可用），不再依赖删除本地文件，与主流启动器行为一致。

### CurseForge 资源接入

**10. CurseForge 三级方案**

- 用户自填 API Key：DPAPI 加密落盘，填一次永久生效
- 内置 Key：构建期注入，Key 不进入源码仓库
- 无 Key 兜底：搜索页提供官网手动下载入口，下载失败也有对应模组官网链接

---

## 稳定性

- 自动化测试 633 项全部通过
- 发布构建签名有效
- 下载引擎逐读采样加固：无界面下载同样具备停滞兜底

## 说明

- 单文件免安装，双击即用
- 本次为迭代更新，功能与修复均经真机验证
