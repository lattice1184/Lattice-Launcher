# 2026-08-09 开发记录（台风夜）

台风白海豚登陆前夜。房间没空调，不插电，全程做「不碰屏幕也能验真机」的事。

## 今天干了什么

### 1. 真实网络探针（无头真机测试）
- 问题：你说「进度条满了还在排队等待，维持十几二十秒」——一直靠代码推理，没跑过真机全链路
- 做法：`launcher/.probe/` 临时控制台工程，**走 GUI 完全相同的代码路径**（EnqueueGroup 组任务 → VersionInstaller → LoaderService），真实网络完整下载 **1.21.10 + Fabric 0.19.3**，自动监听进度监听违规（未完成却 ≥99.999%），退出码 = 完成 ? 0 : 1
- 结果：129.6s 全量下载（client jar + 125 库 + 4403 assets），stage 实时更新，**0 违规**——「排队等待」之前只是探针空跑的产物，修完实测没有

### 2. AL33：聚合层进度提前 100%（就是你说的 109 vs 540）
- 已完成的子任务完成时 Post(100) 会污染加权平均 → 父任务「没下完却 100%」
- 修复：聚合封顶 99%，100 只由完成路径给出。**你在 GUI 看到的「满了还在等」= 旧版行为，新版会 99% → 掉到 ~21%（assets 阶段挂载）→ 爬升 → 瞬间 100%**

### 3. AL34：asm-9.10.1.jar「神秘取消」根因（探针最后一公里）
- 探针第一轮：127/128 文件完成，唯一失败 = asm-9.10.1.jar，状态 Canceled、无错误、不可重试
- 根因：Fabric 库走 maven.fabricmc.net（无镜像 → 单候选直连），某次连接等响应头超过 HttpClient 默认 100s → 抛 TaskCanceledException（取消类异常但**不是用户取消**）→ 下载层没接住 → 任务层误判「用户取消」
- 修复三层：超时转可重试错误（退避重试）；任务层兜底（token 没被请求的取消一律记失败带信息）；组任务同款防御
- 3 个新测试先红后绿；探针重跑 **19.6s 完成、0 违规**

### 4. LoaderService 构造缺陷（探针发现）
- 不传 downloads 时自建 DownloadService 会写 `GameDirectory.Detect()`（真实安装位）→ 下载进错目录 → Verify 报「缺文件」。传 gameDirectory 修复

## 验证状态
- 测试：**338/338 全绿**（335 + 3 新增）
- App 构建：**0 错误**
- 探针：state=Completed，0 违规，versions 下 1.21.10 + fabric-loader-0.19.3-1.21.10 齐全
- 发布：**02:13 完成**（自包含 79.7MB + 轻量版 44.5MB，已签名 + 使用说明.txt）

## 版本统计（自 08-08 联机第一批以来，共 7 版）
| # | 时间 | 版本内容 |
|---|---|---|
| 1 | 08-08 上午 | 联机第一批：LAN 局域网联机（178MB） |
| 2 | 08-08 11:19 | 联机第二批：创建房间 + 防火墙自动放行（178MB） |
| 3 | 08-08 11:30 | 联机 4 槽点修复（复制房间/去开服页引导/版本过滤/下拉框）（186MB） |
| 4 | 08-08 11:50 | 根治「缺文件靠自修复」：安装事务化 + 坏 jar 检测（186MB） |
| 5 | 08-08 22:57 | 体积优化 186MB → 84MB（单文件压缩）+ 双版本发布（+轻量版） |
| 6 | 08-09 00:14 | 深夜批次：联机失败真根因（FireActionAsync）+ CF key 清空 + DPAPI 加密 |
| 7 | 08-09 02:13 | **今晚这版**：AL33 进度封顶 + AL34 超时修复 + LoaderService 构造修复 + 探针 |

## 联机什么时候完成的（回顾）
- **08-08**：S 批次「联机与开服彻底分离 + 陶瓦（Terracotta）联机集成」标了完成（+28 测试，328/328 全绿），T 批次「联机 UI 重做（Tab 切换+动画+配色规范化）」同天完成
- 00:14 那版收尾了联机失败的根因修复（FireActionAsync 只查状态码，不再把动作端点响应当 JSON 解析）
- **真机两机验收还没做**（清单在 SESSION_NOTES S 批次节）——明天就干这个

## 明天待办
1. **两机联机实测**（验收清单 8 项）——EasyTier DNS 隐患预警（建房日志 100 条 `dns lookup_ip failed`）
2. 打包已发，SmartScreen 话术：**「点『仍要运行』正常现象，自签名没买证书」**
3. 探针 .probe/ 在 #221 收尾后删除
4. SHA256 发布脚本（未答复做不做）
