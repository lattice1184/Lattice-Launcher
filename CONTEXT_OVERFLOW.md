# 上下文超限（API 400）应急文档

> **规则**：一旦 Claude Code 会话遇到 API 400 上下文超限（"100% context used" / context_length_exceeded），**立即**在下方"事件记录"节登记一次，然后按"恢复步骤"执行。此文件是流程要求（2026-08-04 用户指令）。

## 事件记录

| # | 时间 | 触发时水位（实际 / 917k） | 触发操作 | 恢复方式 |
|---|------|--------------------------|----------|----------|
| 1 | （示例）2026-08-03 | 82% | 未及时压缩 + 大批次实现 | /compact 后继续 |

## 为什么会发生

- 本模型实际上下文红线 = **917k tokens**（1M 窗口 − 128k max_tokens），自动压缩阈值 983k 太晚
- 状态栏百分比可能按 200K 窗口计算（无 `[1m]` 后缀时）——显示 100% 不代表真的到红线，但**真的到 917k 时 API 直接 400 死锁**
- `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=70` 已设系统环境变量（只能调低不能调高；放 settings.json env 块无效，必须真实环境变量）

## 恢复步骤（400 发生后）

1. **立即停止新工作**，告知用户水位超限
2. **`/compact`**（约 1 分钟，压缩后从摘要继续，工作不丢）——若 /compact 也 400 则 `/clear` 开新会话 + `/resume` 找回旧会话
3. **下个会话恢复模型 `[1m]` 后缀**（Cherry Studio 配置），让状态栏按 1M 显示真实百分比
4. 确认 `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=70` 在环境变量中（新进程生效）

## 预防

- 每完成一个批次 → 立即提交 + 更新 `SESSION_NOTES.md`（压缩会话长度）
- 水位 >600k 提醒 / >700k 建议 /compact / >850k 警告停止
- cron 每 20 分钟水位监控（session-only，7 天过期需重建）
- Bash 重型命令重定向到文件只回尾部；禁止整读大文件；Edit 最小片段
