# Lattice 下一轮功能计划（联机 + 账号 + UI 效果）

> 状态：草稿待确认（2026-08-08）
> 依据：BlockHelm-Launcher 对比报告（`Temp/blockhelm_report.md`）+ 用户已确认"联机和账号登录排进下一轮"

## 总览

BlockHelm 独有四块功能：联机（Terracotta）、账号（MS OAuth/离线/authlib）、双镜像自更新、整合包服务端部署。
我们已有优势：自包含 170MB、双源下载、依赖补全、动画体系（UiAnim 内核 S1-S7）。
下一轮主攻 **账号 + 联机**，UI 效果按需跟进，自更新/服务端部署标记可选。

---

## 一、账号系统（工程量最大，优先）

### 1.1 三种登录方式（完整覆盖）
| 方式 | 说明 | 工程量 |
|---|---|---|
| 微软正版 OAuth | MSAL 码流：启动器开本地端口 → 浏览器登录 → 回调收 token → 刷新 token 持久化 → 验证游戏所有权（entitlement） | 大 |
| 离线账号 | 自定义用户名 + UUID 生成（多种模式：随机/名字哈希/固定），Yggdrasil 假 token 启动 | 小 |
| authlib-injector 第三方 | 自建站/第三方皮肤站（如 LittleSkin）：认证地址 + Yggdrasil 流程 | 中 |

### 1.2 凭据安全
- **DPAPI 加密存储**（Windows 原生当前用户加密）：token 不落明文
- 刷新 token 单独存，启动时静默刷新，过期才重新登录
- 皮肤/头像缓存（BHL 有 Cape 披风服务，我们做不做看用户）

### 1.3 UI
- 首页/顶部显示当前账号（头像 + 名字 + 登录方式标签）
- 账号管理页：列表 + 添加（三种类型对话框）+ 删除 + 重命名
- 启动时自动选择账号，未登录走离线兜底

### 1.4 启动接入
- 启动参数注入：accessToken / uuid / username → X-Auth 参数
- authlib-injector：下载 injector jar 到运行库 + 加 JVM 参数

---

## 二、联机功能（Terracotta 式）

### 2.1 局域网发现
- UDP 广播发现局域网内的 Lattice 玩家（带版本/世界名信息）
- 一键加入，启动参数 `--server 主机IP:端口`

### 2.2 房间码跨网联机
- 客户端生成房间码（短码）→ 通过 Terracotta 服务中转 → 另一玩家输码加入
- 实际连接为玩家间直连（P2P），服务端只做信令
- 需考虑：无公网 IP 时的 NAT 穿透（Terracotta 服务端 + 可能的中继）

### 2.3 UI
- 联机页：本地房间列表 + 创建房间 + 加入房间（输码/输 IP）
- 协议同意对话框（首次使用条款）

### 2.4 边界
- 只负责"把玩家送进同一个服务器"，服务器本体仍由开房间的人跑
- 房间主需要能启动服务器（我们已有服务器启动能力，ServerMemoryMb 等设置已存在 ✓）

---

## 三、UI 与效果（插空做，不阻塞主线）

### 3.1 主题
- 强调色 9 色（BHL 有，我们 5 色，扩到 9：Amber/Blue/Cyan/Emerald/Green/Orange/Pink/Purple + 现有青绿）
- 明暗双主题切换（BHL 有 Dark/Light，我们现在只有暗色）
- 背景图 + 高斯模糊（BHL 有 HLSL ProgressiveGaussianBlur；Avalonia 可用现有模糊能力先做简化版）

### 3.2 动画补全
- 页面切换过渡（BHL PageTransitionService，我们有 UiAnim 内核，加整体切换）
- 折叠动画已在 S7 完成，后续补：列表项进入动画、Toast 增强

### 3.3 多语言（可选，工程量中）
- BHL 4 语言（简中/繁中/英文/日语），我们是全中文硬编码——上多语言要全量抽取字符串，工作量大，建议放后面

### 3.4 自更新（可选）
- GitHub + Gitee 双镜像 manifest 更新通道（BHL 方案）
- 发布铁律不变（PCL 子目录挪出挪回），更新器要处理杀进程/回滚

---

## 四、分批建议

- **第一批（核心）**：离线账号 + 微软 OAuth + 账号 UI + 启动参数接入 → 联机页 + 局域网发现 + 房间码
- **第二批**：authlib-injector 第三方 + 主题扩展（9 色 + 背景模糊）
- **可选/暂缓**：自更新双镜像、多语言全量、整合包服务端部署（工程量最大，BHL 也有服务端组件）

## 五、风险与注意

- 微软 OAuth 需要注册 Azure 应用拿 Client ID（免费，要填本地跳转 URI）——和 CF key 类似有外网注册环节，用户要有梯子
- Terracotta 服务端信令依赖外部服务，若自建要一台公网服务器；先用公共 Terracotta 实例验证
- 账号 token 安全：只进内存 + DPAPI 落盘，日志永远不打印 token

## 六、验证

- 每批 build 0 错误 → 发布.ps1（PCL 挪出挪回铁律）→ 真机验收
- 联机验收：两台设备（或一台开服一台加入）实测
- 账号验收：离线先通，OAuth 用真微软账号实测
